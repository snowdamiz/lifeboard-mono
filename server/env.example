# ===========================================
# MegaPlanner Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# cp env.example .env

# -------------------------------------------
# Google OAuth 2.0
# -------------------------------------------
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project (or select existing)
# 3. Go to "APIs & Services" > "Credentials"
# 4. Click "Create Credentials" > "OAuth client ID"
# 5. Select "Web application"
# 6. Add authorized redirect URI: http://localhost:4000/auth/google/callback
# 7. Copy the Client ID and Client Secret below

GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# -------------------------------------------
# Security Keys
# -------------------------------------------
# Generate these with: mix phx.gen.secret
# Run the command twice to get two different keys

# Used for signing cookies and sessions
SECRET_KEY_BASE=generate_a_64_char_secret_key_using_mix_phx_gen_secret_command_here

# Used for JWT token signing (Guardian)
GUARDIAN_SECRET_KEY=generate_another_64_char_secret_key_using_mix_phx_gen_secret_here

# -------------------------------------------
# Database (Production Only)
# -------------------------------------------
# For local development, the defaults in dev.exs are used
# For production, set your database connection string

# DATABASE_URL=ecto://username:password@hostname:5432/mega_planner_prod

# -------------------------------------------
# Production Settings (Optional)
# -------------------------------------------
# PHX_HOST=your-app.fly.dev
# PORT=4000
# POOL_SIZE=10

# Frontend URL for OAuth redirects (in production)
# FRONTEND_URL=https://your-frontend-domain.com

