---
phase: 03-application-code-fixes
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/mega_planner/inventory/shopping_list_item.ex
  - server/lib/mega_planner/inventory/item.ex
  - server/lib/mega_planner/inventory/shopping_list.ex
  - server/lib/mega_planner/inventory/sheet.ex
  - server/lib/mega_planner/accounts/user.ex
  - server/lib/mega_planner/accounts/user_preferences.ex
  - server/lib/mega_planner/budget/source.ex
  - server/lib/mega_planner/budget/entry.ex
  - server/lib/mega_planner/notes/notebook.ex
  - server/lib/mega_planner/notes/page.ex
  - server/lib/mega_planner/notes/page_link.ex
  - server/lib/mega_planner/notifications/notification.ex
  - server/lib/mega_planner/notifications/preferences.ex
  - server/lib/mega_planner/households/invitation.ex
  - server/lib/mega_planner/tags/tag.ex
autonomous: true
requirements:
  - CODE-03

must_haves:
  truths:
    - "No foreign_key_constraint calls remain in inventory/ accounts/ budget/ notes/ notifications/ households/ tags/ schema files"
    - "Each removed FK constraint is replaced with validate_change/3 that does a Repo.get for the referenced module"
    - "mix compile succeeds after all changes"
  artifacts:
    - path: "server/lib/mega_planner/inventory/shopping_list_item.ex"
      provides: "validate_change/3 for shopping_list_id, inventory_item_id, user_id, household_id"
      contains: "validate_change"
    - path: "server/lib/mega_planner/accounts/user.ex"
      provides: "validate_change/3 for household_id"
      contains: "validate_change"
    - path: "server/lib/mega_planner/budget/entry.ex"
      provides: "validate_change/3 for source_id, user_id, household_id"
      contains: "validate_change"
  key_links:
    - from: "server/lib/mega_planner/inventory/shopping_list_item.ex"
      to: "MegaPlanner.Repo.get/2"
      via: "validate_change/3 pre-insert check"
      pattern: "validate_change"
---

<objective>
Replace all remaining `foreign_key_constraint/3` calls in inventory, accounts, budget, notes, notifications, households, and tags schema modules with `validate_change/3` pre-insert existence checks using `Repo.get/2`.

Purpose: Completes the CODE-03 sweep begun in plan 03-02. After this plan, zero `foreign_key_constraint` calls remain anywhere in `lib/` — eliminating all latent `Ecto.ConstraintError` 500 risks on SQLite.

Output: 15 files updated. 27 FK constraint calls replaced across inventory (8), accounts (2), budget (5), notes (4), notifications (2), households (2), and tags (2) domains.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-application-code-fixes/03-RESEARCH.md

<interfaces>
<!-- FK constraint replacement pattern (identical to 03-02) -->
<!-- BEFORE: |> foreign_key_constraint(:field_name) -->
<!-- AFTER:  |> validate_change(:field_name, fn :field_name, id -> -->
<!--              if MegaPlanner.Repo.get(TargetModule, id), -->
<!--                do: [], -->
<!--                else: [field_name: "does not exist"] -->
<!--            end) -->

<!-- Module-to-schema map for this plan's files: -->
<!-- :household_id      -> MegaPlanner.Households.Household -->
<!-- :user_id           -> MegaPlanner.Accounts.User -->
<!-- :shopping_list_id  -> MegaPlanner.Inventory.ShoppingList -->
<!-- :inventory_item_id -> MegaPlanner.Inventory.Item -->
<!-- :sheet_id          -> MegaPlanner.Inventory.Sheet (item.ex) -->
<!-- :source_id         -> MegaPlanner.Budget.Source (entry.ex) -->
<!-- :notebook_id       -> MegaPlanner.Notes.Notebook (page.ex) -->
<!-- :page_id           -> MegaPlanner.Notes.Page (page_link.ex) -->
<!-- :inviter_id        -> MegaPlanner.Accounts.User (invitation.ex) -->

Important: validate_change/3 is already available via `import Ecto.Changeset` which is present
in all schema files. Do NOT add duplicate imports.

For optional FK fields (nil is valid), validate_change/3 naturally handles this —
it only executes the callback when the field is present in the changeset AND non-nil.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace FK constraints in inventory and accounts schemas (6 files)</name>
  <files>
    server/lib/mega_planner/inventory/shopping_list_item.ex,
    server/lib/mega_planner/inventory/item.ex,
    server/lib/mega_planner/inventory/shopping_list.ex,
    server/lib/mega_planner/inventory/sheet.ex,
    server/lib/mega_planner/accounts/user.ex,
    server/lib/mega_planner/accounts/user_preferences.ex
  </files>
  <action>
Read each file, then rewrite it replacing every `foreign_key_constraint/3` call with the equivalent `validate_change/3` block.

**inventory/shopping_list_item.ex** — replace 4 FK constraints:
- `:shopping_list_id` → `MegaPlanner.Inventory.ShoppingList`
- `:inventory_item_id` → `MegaPlanner.Inventory.Item` (optional field)
- `:user_id` → `MegaPlanner.Accounts.User`
- `:household_id` → `MegaPlanner.Households.Household`

**inventory/item.ex** — replace 1 FK constraint:
- `:sheet_id` → `MegaPlanner.Inventory.Sheet`

**inventory/shopping_list.ex** — replace 2 FK constraints:
- `:household_id` → `MegaPlanner.Households.Household`
- `:user_id` → `MegaPlanner.Accounts.User`

**inventory/sheet.ex** — replace 2 FK constraints:
- `:user_id` → `MegaPlanner.Accounts.User`
- `:household_id` → `MegaPlanner.Households.Household`

**accounts/user.ex** — replace 1 FK constraint:
- `:household_id` → `MegaPlanner.Households.Household`

**accounts/user_preferences.ex** — replace 1 FK constraint:
- `:user_id` → `MegaPlanner.Accounts.User`

Total: 11 FK constraints replaced across inventory and accounts domains.
  </action>
  <verify>
    <automated>grep -rn "foreign_key_constraint" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/mega_planner/inventory/ /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/mega_planner/accounts/ | wc -l</automated>
  </verify>
  <done>Command returns 0. All 11 FK constraints in inventory/ and accounts/ replaced with validate_change/3. `cd /Users/sn0w/Documents/dev/lifeboard-mono/server && mix compile` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Replace FK constraints in budget, notes, notifications, households, and tags schemas (9 files)</name>
  <files>
    server/lib/mega_planner/budget/source.ex,
    server/lib/mega_planner/budget/entry.ex,
    server/lib/mega_planner/notes/notebook.ex,
    server/lib/mega_planner/notes/page.ex,
    server/lib/mega_planner/notes/page_link.ex,
    server/lib/mega_planner/notifications/notification.ex,
    server/lib/mega_planner/notifications/preferences.ex,
    server/lib/mega_planner/households/invitation.ex,
    server/lib/mega_planner/tags/tag.ex
  </files>
  <action>
Read each file, then rewrite it replacing every `foreign_key_constraint/3` call with the equivalent `validate_change/3` block.

**budget/source.ex** — replace 2 FK constraints:
- `:user_id` → `MegaPlanner.Accounts.User`
- `:household_id` → `MegaPlanner.Households.Household`

**budget/entry.ex** — replace 3 FK constraints:
- `:source_id` → `MegaPlanner.Budget.Source` (optional field)
- `:user_id` → `MegaPlanner.Accounts.User`
- `:household_id` → `MegaPlanner.Households.Household`

**notes/notebook.ex** — replace 2 FK constraints:
- `:user_id` → `MegaPlanner.Accounts.User`
- `:household_id` → `MegaPlanner.Households.Household`

**notes/page.ex** — replace 2 FK constraints:
- `:notebook_id` → `MegaPlanner.Notes.Notebook`
- `:user_id` → `MegaPlanner.Accounts.User`

**notes/page_link.ex** — replace 1 FK constraint:
- `:page_id` → `MegaPlanner.Notes.Page`

**notifications/notification.ex** — replace 1 FK constraint:
- `:household_id` → `MegaPlanner.Households.Household`
(Note: user_id is required and present in cast but has no foreign_key_constraint — do not add one.)

**notifications/preferences.ex** — replace 1 FK constraint:
- `:household_id` → `MegaPlanner.Households.Household`

**households/invitation.ex** — replace 2 FK constraints:
- `:household_id` → `MegaPlanner.Households.Household`
- `:inviter_id` → `MegaPlanner.Accounts.User`

**tags/tag.ex** — replace 2 FK constraints:
- `:user_id` → `MegaPlanner.Accounts.User`
- `:household_id` → `MegaPlanner.Households.Household`

Total: 16 FK constraints replaced across budget, notes, notifications, households, and tags domains.
  </action>
  <verify>
    <automated>grep -rn "foreign_key_constraint" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/ | wc -l</automated>
  </verify>
  <done>Command returns 0. Zero foreign_key_constraint calls remain anywhere in lib/. `cd /Users/sn0w/Documents/dev/lifeboard-mono/server && mix compile` succeeds.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "foreign_key_constraint" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/ | wc -l` returns 0
2. `cd /Users/sn0w/Documents/dev/lifeboard-mono/server && mix compile` exits 0 with no errors
</verification>

<success_criteria>
- All 27 remaining foreign_key_constraint calls removed from inventory, accounts, budget, notes, notifications, households, and tags schema files
- Zero foreign_key_constraint calls remain anywhere in lib/ (confirmed by grep)
- mix compile succeeds across all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/03-application-code-fixes/03-03-SUMMARY.md`
</output>
