---
phase: 03-application-code-fixes
plan: "04"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified: []
autonomous: false
requirements:
  - CODE-04

must_haves:
  truths:
    - "grep -rn 'ilike' server/lib/ returns zero results"
    - "grep -rn 'foreign_key_constraint' server/lib/ returns zero results"
    - "No JSONB operators (->, ->>, @>, ?) exist in any fragment() call in lib/"
    - "mix test compiles and runs without adapter-incompatibility failures"
  artifacts:
    - path: "server/test/support/data_case.ex"
      provides: "MegaPlanner.DataCase used by bug_repro tests"
      exports: ["MegaPlanner.DataCase"]
    - path: "server/test/support/fixtures/accounts_fixtures.ex"
      provides: "user_fixture/0 factory"
      exports: ["MegaPlanner.AccountsFixtures"]
  key_links:
    - from: "mix test run"
      to: "SQLite test database"
      via: "Ecto.Adapters.SQL.Sandbox in :manual mode"
      pattern: "Sandbox\\.start_owner"
---

<objective>
Verify CODE-04 (no JSONB operators in lib/) and run the full test suite to confirm the application compiles and tests pass after Wave 1 fixes. This plan is the integration gate for Phase 3.

Purpose: Confirms that all four CODE requirements are satisfied as a unit. CODE-04 is a verification-only requirement (already confirmed clean by research). The `mix test` run validates CODE-01, CODE-02, and CODE-03 fixes together.

Output: Documented verification that grep confirms zero JSONB operators, and that mix test runs without adapter errors. Human checkpoint to review test results.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-application-code-fixes/03-01-SUMMARY.md
@.planning/phases/03-application-code-fixes/03-02-SUMMARY.md
@.planning/phases/03-application-code-fixes/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CODE-04 — no JSONB operators in lib/</name>
  <files></files>
  <action>
Run these three grep commands and capture output. All must return zero matching lines with JSONB operators:

1. Check for arrow operators in fragment strings:
   `grep -rn "fragment" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/ | grep -E '"->>|"->|@>|"\?'`

2. Check for any remaining ilike:
   `grep -rn "ilike" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/`

3. Check for any remaining foreign_key_constraint:
   `grep -rn "foreign_key_constraint" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/`

Document the output of each command. If any command returns results, do NOT proceed to Task 2 — fix the issues first by re-reading the relevant plan (03-01, 03-02, or 03-03) and re-applying the changes.

Expected results:
- Command 1: zero lines (no JSONB operators — research confirmed this; grep is a verification pass)
- Command 2: zero lines (CODE-01 complete)
- Command 3: zero lines (CODE-03 complete)
  </action>
  <verify>
    <automated>grep -rn "ilike" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/ | wc -l && grep -rn "foreign_key_constraint" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/ | wc -l</automated>
  </verify>
  <done>Both commands return 0. No ilike, no foreign_key_constraint, no JSONB operators remain in lib/.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite</name>
  <files></files>
  <action>
Run the full test suite from the server directory:

```
cd /Users/sn0w/Documents/dev/lifeboard-mono/server && mix test
```

The `mix test` alias in mix.exs runs `ecto.create --quiet`, `ecto.migrate --quiet`, then `test` — this exercises the full pipeline.

Capture and record:
- Whether compilation succeeds (no missing module errors)
- Number of tests run and pass/fail counts
- Any failures and their error messages

Expected: All tests compile and run. Failures from pre-existing bugs (like the bug_repro_trip_merge_test.exs which intentionally tests buggy behavior) are acceptable — look specifically for:
- NO `Ecto.QueryError` with "ilike is not supported by SQLite3"
- NO `cannot compile module ... module MegaPlanner.DataCase is not loaded`
- NO `(Ecto.ConstraintError) constraint error when attempting to insert struct`
- NO `async: true` concurrency failures (DBConnection.ConnectionError)

If any of those four error patterns appear, fix them before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/sn0w/Documents/dev/lifeboard-mono/server && mix test 2>&1 | tail -20</automated>
  </verify>
  <done>mix test runs without compilation errors. No adapter-incompatibility failures present (ilike errors, DataCase missing, ConstraintError, async concurrency). Any remaining failures are pre-existing application bugs, not adapter issues.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — Phase 3 integration gate</name>
  <files></files>
  <action>Pause for human to confirm all Phase 3 CODE fixes are complete and mix test is clean of adapter errors.</action>
  <verify>
    <automated>grep -rn "ilike" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/ | wc -l</automated>
  </verify>
  <done>Human confirms: zero ilike, zero foreign_key_constraint, zero async:true in test files, DataCase and AccountsFixtures exist, mix test runs without adapter errors.</done>
  <what-built>
    Wave 1 CODE fixes:
    - CODE-01: All 31 ilike → like replacements in search.ex, receipts.ex, goals.ex, inventory.ex, templates.ex
    - CODE-02: DataCase and AccountsFixtures created; async: false on all DB test files
    - CODE-03: All 54 foreign_key_constraint calls replaced with validate_change/3 across 31 schema files
    - CODE-04: Verified clean — no JSONB operators in lib/ fragments
  </what-built>
  <how-to-verify>
    1. Run: `grep -rn "ilike" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/ | wc -l` — must return 0
    2. Run: `grep -rn "foreign_key_constraint" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/ | wc -l` — must return 0
    3. Run: `grep -rn "async: true" /Users/sn0w/Documents/dev/lifeboard-mono/server/test/ | wc -l` — must return 0
    4. Check: `ls /Users/sn0w/Documents/dev/lifeboard-mono/server/test/support/` — must show data_case.ex and fixtures/
    5. Review test output from Task 2 — confirm no ilike/DataCase/ConstraintError/async errors appear
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 3 complete when all of these pass:
1. `grep -rn "ilike" server/lib/ | wc -l` → 0
2. `grep -rn "foreign_key_constraint" server/lib/ | wc -l` → 0
3. `grep -rn "async: true" server/test/ | wc -l` → 0
4. `mix test` compiles and runs without adapter-incompatibility errors
5. Human checkpoint approved
</verification>

<success_criteria>
- CODE-01: Zero ilike calls in lib/ (grep confirms)
- CODE-02: mix test compiles without DataCase/AccountsFixtures errors; all DB tests use async: false
- CODE-03: Zero foreign_key_constraint calls in lib/ (grep confirms)
- CODE-04: Zero JSONB operators in lib/ fragment strings (grep confirms)
- mix test passes the adapter-compatibility bar (no SQLite-specific Ecto errors)
</success_criteria>

<output>
After completion, create `.planning/phases/03-application-code-fixes/03-04-SUMMARY.md`
</output>
