---
phase: 04-data-migration-pipeline
plan: "04"
type: execute
wave: 3
depends_on: ["04-03"]
files_modified: []
autonomous: false
requirements: [DATA-05]

must_haves:
  truths:
    - "mix migrate.import completes without errors against the local SQLite database"
    - "PRAGMA foreign_key_check returns zero violations after import"
    - "mix migrate.verify reports zero discrepancies across all 48 tables"
    - "SQLite file contains all production rows — record counts match PostgreSQL"
    - "Spot checks on goals.linked_task_ids, habit_inventories.linked_inventory_ids, brands.default_tags confirm readable UUID strings"
  artifacts:
    - path: "server/mega_planner_dev.db"
      provides: "SQLite database populated with all production data"
      contains: "all 48 tables with production record counts"
  key_links:
    - from: "mix migrate.import"
      to: "server/mega_planner_dev.db"
      via: "MegaPlanner.Repo Repo.insert_all with PRAGMA foreign_keys = OFF"
      pattern: "insert_all"
    - from: "mix migrate.verify"
      to: "POSTGRES_URL + MegaPlanner.Repo"
      via: "COUNT(*) on each table in both databases"
      pattern: "VERIFY: PASSED"
---

<objective>
Run `mix migrate.import` to populate the local SQLite database with all production data, then run `mix migrate.verify` to confirm zero discrepancies.

Purpose: DATA-05 — this is the final validation step of Phase 4. A passing verify run proves that zero records were lost in the migration pipeline. This is the core non-negotiable constraint of the entire project.
Output: Local `mega_planner_dev.db` fully populated with production data; verify report showing all 48 tables match.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-migration-pipeline/04-RESEARCH.md
@.planning/phases/04-data-migration-pipeline/04-03-SUMMARY.md

<interfaces>
<!-- Commands for this plan. No code changes — execution only. -->

Import command (run from server/ directory):
```
cd /Users/sn0w/Documents/dev/lifeboard-mono/server
mix migrate.import /path/to/migration_export.json
```
Expected output:
- Lines: "  users: N rows", "  tasks: N rows", ... for all 48 tables
- "PRAGMA foreign_key_check: PASSED — zero violations"
- "Import complete."

Verify command (requires fly proxy tunnel and POSTGRES_URL — same as export):
```
cd /Users/sn0w/Documents/dev/lifeboard-mono/server
POSTGRES_URL="ecto://mega_planner_api:PASSWORD@localhost:5432/mega_planner_api" \
  mix migrate.verify
```
Expected output:
- Lines: "OK users: N", "OK tasks: N", ... for all tables
- "VERIFY: PASSED — all 48 tables match"

Spot check UUID arrays in SQLite:
```
sqlite3 /Users/sn0w/Documents/dev/lifeboard-mono/server/mega_planner_dev.db \
  "SELECT linked_task_ids FROM goals WHERE linked_task_ids != '[]' LIMIT 3"
# Expected: JSON strings like '["uuid-string-1","uuid-string-2"]'
# NOT: binary blobs or empty results when data should exist

sqlite3 /Users/sn0w/Documents/dev/lifeboard-mono/server/mega_planner_dev.db \
  "SELECT default_tags FROM brands WHERE default_tags != '[]' LIMIT 3"
```

PRAGMA foreign_key_check (direct SQLite verification):
```
sqlite3 /Users/sn0w/Documents/dev/lifeboard-mono/server/mega_planner_dev.db \
  "PRAGMA foreign_key_check"
# Expected: empty output (zero rows)
```

If import fails with "table has N columns but M were supplied":
- A column in the JSON export has a name that doesn't match the current SQLite schema
- Check which table failed; compare export column names vs PRAGMA table_info output
- May indicate a column was renamed or the export caught a schema mismatch

If import fails with duplicate primary key errors despite on_conflict: :nothing:
- The SQLite database was not reset before import
- Run: cd server && mix ecto.reset (this drops and recreates all tables)
- Then re-run mix migrate.import
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reset SQLite and run migrate.import</name>
  <files></files>
  <action>
    Before importing, ensure the local SQLite database is in a clean state. Run:
    ```
    cd /Users/sn0w/Documents/dev/lifeboard-mono/server && mix ecto.reset
    ```
    This drops the existing SQLite file, creates a fresh one, and runs all migrations — producing an empty schema with correct tables.

    Then run the import using the export file path from the 04-03-SUMMARY.md (check for the path recorded there):
    ```
    cd /Users/sn0w/Documents/dev/lifeboard-mono/server && \
      mix migrate.import /path/to/migration_export.json
    ```

    Watch for:
    - Progress lines for all 48 tables — confirm counts are non-zero for the tables that should have data
    - "PRAGMA foreign_key_check: PASSED — zero violations" at the end
    - No crash or exception before "Import complete."

    If `mix ecto.reset` fails: check that no other process has the SQLite file open (e.g., a running Phoenix server process). Stop any running `mix phx.server` first.

    If import crashes on a specific table with a column mismatch error, note the table name and the column mentioned in the error and stop — do not attempt partial fixes. Report the error and the table name as the outcome so a gap-closure plan can be created.
  </action>
  <verify>
    <automated>cd /Users/sn0w/Documents/dev/lifeboard-mono/server && sqlite3 mega_planner_dev.db "SELECT COUNT(*) FROM tasks"</automated>
  </verify>
  <done>Import completes without error; "PRAGMA foreign_key_check: PASSED" appears in output; tasks table has non-zero row count in SQLite</done>
</task>

<task type="auto">
  <name>Task 2: Run migrate.verify and confirm zero discrepancies</name>
  <files></files>
  <action>
    Run the verify task with the same POSTGRES_URL used during export (fly proxy tunnel must be running):
    ```
    cd /Users/sn0w/Documents/dev/lifeboard-mono/server && \
      POSTGRES_URL="ecto://mega_planner_api:PASSWORD@localhost:5432/mega_planner_api" \
      mix migrate.verify
    ```

    Replace PASSWORD with the actual password retrieved during Plan 04-03.

    Expected output: Every line starts with "OK" and the final line is "VERIFY: PASSED — all 48 tables match".

    If any line starts with "MISMATCH", note the table name and the pg vs sqlite counts — this indicates rows that were not imported. Do not attempt to manually fix count discrepancies. Record the mismatch details as the outcome.

    After verify passes, run the UUID array spot checks to confirm the ROADMAP success criteria item 4:
    ```
    sqlite3 /Users/sn0w/Documents/dev/lifeboard-mono/server/mega_planner_dev.db \
      "SELECT linked_task_ids FROM goals WHERE linked_task_ids NOT IN ('[]', 'null') LIMIT 3"

    sqlite3 /Users/sn0w/Documents/dev/lifeboard-mono/server/mega_planner_dev.db \
      "SELECT default_tags FROM brands WHERE default_tags NOT IN ('[]', 'null') LIMIT 3"
    ```

    Finally, run PRAGMA foreign_key_check as a belt-and-suspenders check:
    ```
    sqlite3 /Users/sn0w/Documents/dev/lifeboard-mono/server/mega_planner_dev.db \
      "PRAGMA foreign_key_check"
    ```
    Expected: empty output (no rows returned).
  </action>
  <verify>
    <automated>cd /Users/sn0w/Documents/dev/lifeboard-mono/server && sqlite3 mega_planner_dev.db "PRAGMA foreign_key_check"</automated>
  </verify>
  <done>PRAGMA foreign_key_check returns empty (zero rows); `mix migrate.verify` exits with "VERIFY: PASSED"; UUID array spot checks show JSON strings not binary blobs</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase 4 integration gate — confirm all five DATA requirements satisfied</name>
  <files></files>
  <action>Pause for human to confirm all five Phase 4 DATA requirements are satisfied based on the Task 1 and Task 2 output above.</action>
  <verify>
    <automated>cd /Users/sn0w/Documents/dev/lifeboard-mono/server && sqlite3 mega_planner_dev.db "PRAGMA foreign_key_check"</automated>
  </verify>
  <done>Human confirms: mix migrate.verify passed with zero discrepancies; PRAGMA foreign_key_check returned zero rows; UUID arrays are readable strings; all five Phase 4 success criteria satisfied.</done>
  <what-built>
    Import and verify pipeline has run. Claude has executed:
    1. `mix ecto.reset` — fresh SQLite schema
    2. `mix migrate.import <export_path>` — all 48 tables populated
    3. `PRAGMA foreign_key_check` — zero violations
    4. `mix migrate.verify` — count comparison across PostgreSQL and SQLite
    5. UUID array spot checks — readable string format confirmed
  </what-built>
  <how-to-verify>
    Review the output from Task 1 and Task 2 above.

    **Confirm all five Phase 4 success criteria are met:**

    1. `mix migrate.export` ran against production PostgreSQL and produced a complete JSON export covering all 48 tables (verified in Plan 04-03)
    2. `mix migrate.import` inserted all exported rows including two-pass for self-referential and circular FKs
    3. `mix migrate.verify` reports zero discrepancies across all table record counts
    4. Spot checks on `goals.linked_task_ids`, `habit_inventories.linked_inventory_ids`, `brands.default_tags` confirm UUID values are readable strings
    5. `PRAGMA foreign_key_check` on the SQLite file returns zero rows

    If any criterion is not met, describe what failed so a gap-closure plan can be created.

    Type "approved" if all five criteria are satisfied.
    Or describe which criterion failed and what the actual output was.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 4, or describe failures</resume-signal>
</task>

</tasks>

<verification>
Phase 4 complete when:
- `mix migrate.verify` output ends with "VERIFY: PASSED — all 48 tables match"
- `sqlite3 mega_planner_dev.db "PRAGMA foreign_key_check"` returns empty
- UUID array spot checks show JSON strings
- Human checkpoint approved
</verification>

<success_criteria>
- Local `mega_planner_dev.db` contains all production data
- Zero count discrepancies across all 48 tables
- Zero FK violations in PRAGMA foreign_key_check
- UUID array columns confirmed as readable strings
- Phase 4 human checkpoint approved — ready for Phase 5 (Fly.io deployment)
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-migration-pipeline/04-04-SUMMARY.md`
Include the verify output (table counts summary) and confirmation of all five Phase 4 success criteria.
</output>
