---
phase: 04-data-migration-pipeline
plan: "03"
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: []
autonomous: false
requirements: [DATA-04]

user_setup:
  - service: fly_postgres
    why: "Production PostgreSQL data export requires a local tunnel to the Fly.io Postgres cluster"
    env_vars:
      - name: POSTGRES_URL
        source: "Derived from DATABASE_URL on the mega-planner-api Fly app. Retrieve with: fly ssh console -a mega-planner-api -C 'printenv DATABASE_URL' — this returns an ecto:// URL. Substitute host with 'localhost' and keep the rest."
    dashboard_config:
      - task: "Start fly proxy tunnel before running export"
        location: "Terminal: fly proxy 5432:5432 -a mega-planner-api-db (keep this process running while export runs)"

must_haves:
  truths:
    - "A JSON export file exists at a local path with non-zero size"
    - "The export file contains a 'tables' key with exactly 48 table subkeys"
    - "The exported rows are complete — no serialization errors during export"
    - "UUID array columns (goals.linked_task_ids, habit_inventories.linked_inventory_ids, brands.default_tags) contain readable UUID strings, not binary blobs"
  artifacts:
    - path: "migration_export.json"
      provides: "Complete PostgreSQL export — all 48 tables — ready for import"
      contains: "exported_at"
  key_links:
    - from: "fly proxy 5432:5432 -a mega-planner-api-db"
      to: "POSTGRES_URL=ecto://...@localhost:5432/mega_planner_api mix migrate.export"
      via: "WireGuard tunnel on local port 5432"
      pattern: "localhost:5432"
---

<objective>
Run `mix migrate.export` against the live production PostgreSQL database via fly proxy tunnel and produce a verified export file.

Purpose: DATA-04 — this is the one-shot operation that captures all production data. The export file is the source of truth for the import in Plan 04-04. Getting this right is critical: if serialization fails, records are lost; if the connection drops mid-export, the file is incomplete.
Output: `migration_export.json` (or custom path specified by user) containing all 48 tables' data from production PostgreSQL.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-migration-pipeline/04-RESEARCH.md
@.planning/phases/04-data-migration-pipeline/04-01-SUMMARY.md
@.planning/phases/04-data-migration-pipeline/04-02-SUMMARY.md

<interfaces>
<!-- Production access details from research -->

Fly app name: mega-planner-api
Postgres cluster: mega-planner-api-db
Database name: mega_planner_api
Username: mega_planner_api (matches app name)

Tunnel command (run in a separate terminal — keep it running during export):
fly proxy 5432:5432 -a mega-planner-api-db

Password retrieval (run after establishing tunnel):
fly ssh console -a mega-planner-api -C 'printenv DATABASE_URL'
This returns an ecto:// URL with the actual password. Replace the host in that URL with 'localhost' to get POSTGRES_URL.

Export command format:
POSTGRES_URL="ecto://mega_planner_api:<password>@localhost:5432/mega_planner_api" \
  mix migrate.export /path/to/migration_export.json

Verification commands:
# Check file size (should be several MB for a real database):
ls -lh /path/to/migration_export.json

# Check table count (should be 48):
jq '.tables | keys | length' /path/to/migration_export.json

# Check UUID array columns for readable strings:
jq '.tables.goals[0].linked_task_ids' /path/to/migration_export.json
jq '.tables.brands[0].default_tags' /path/to/migration_export.json
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Run production export via fly proxy tunnel</name>
  <files></files>
  <action>Pause for human to establish fly proxy tunnel, retrieve POSTGRES_URL, and run mix migrate.export against production PostgreSQL. See how-to-verify for exact steps.</action>
  <verify>
    <automated>MISSING — export file path is determined by human during this checkpoint; verification is manual (jq table count check)</automated>
  </verify>
  <done>Export file exists at a local path; `jq '.tables | keys | length' <path>` returns 48; file size is non-trivial (greater than 10KB); no serialization errors during export run.</done>
  <what-built>Mix tasks from plans 04-01 and 04-02 are compiled and ready. The export task requires a live PostgreSQL connection via fly proxy tunnel, which Claude cannot establish autonomously (requires Fly CLI authentication and a running process).</what-built>
  <how-to-verify>
    Complete these steps:

    **Step 1 — Start the fly proxy tunnel (keep running in a separate terminal tab):**
    ```
    fly proxy 5432:5432 -a mega-planner-api-db
    ```
    Expected output: "Proxying local port 5432 to remote [...]"

    **Step 2 — Retrieve the PostgreSQL password:**
    ```
    fly ssh console -a mega-planner-api -C 'printenv DATABASE_URL'
    ```
    This prints an ecto:// URL like: `ecto://mega_planner_api:PASSWORD@top2.nearest.of.mega-planner-api-db.internal:5432/mega_planner_api`
    Take note of PASSWORD.

    **Step 3 — Run the export (from the server/ directory):**
    ```
    cd /Users/sn0w/Documents/dev/lifeboard-mono/server
    POSTGRES_URL="ecto://mega_planner_api:PASSWORD@localhost:5432/mega_planner_api" \
      mix migrate.export /tmp/migration_export.json
    ```
    Expected output: lines like "  users: 3 rows", "  tasks: 47 rows", ..., "Export complete: /tmp/migration_export.json (48 tables)"

    **Step 4 — Spot-check the export file:**
    ```
    ls -lh /tmp/migration_export.json
    jq '.tables | keys | length' /tmp/migration_export.json
    jq '.tables.goals[0].linked_task_ids' /tmp/migration_export.json
    jq '.tables.brands[0].default_tags' /tmp/migration_export.json
    ```
    Expected: file size > 10KB; table count = 48; UUID arrays show strings like `["uuid1", "uuid2"]` (not binary)

    **Step 5 — Copy the export file to the project directory for safekeeping:**
    ```
    cp /tmp/migration_export.json /Users/sn0w/Documents/dev/lifeboard-mono/migration_export.json
    ```

    **If export fails:**
    - `Postgrex.Error: connection refused` → fly proxy is not running or tunnel is on wrong port
    - `POSTGRES_URL is required` → env var not set correctly
    - `Protocol.UndefinedError for Jason.Encoder` → a column type is not handled by serialize/1; note which table and type, then describe the issue

    Type "approved" when the export file exists and the 48-table spot check passes.
    Or describe any errors encountered.
  </how-to-verify>
  <resume-signal>Type "approved" with the export file path, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
After checkpoint approval:
- Export file exists at the path provided by user
- `jq '.tables | keys | length' <path>` = 48
- `ls -lh <path>` shows non-trivial file size (> 10KB for any real database)
- UUID array columns show string values, not nil or binary
</verification>

<success_criteria>
- Production export file exists with non-zero size
- 48 table keys present in the export JSON
- No serialization errors during export run
- UUID array spot checks confirm readable strings
- Export file path recorded for use in Plan 04-04
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-migration-pipeline/04-03-SUMMARY.md`
Include the export file path in the summary for Plan 04-04 to reference.
</output>
