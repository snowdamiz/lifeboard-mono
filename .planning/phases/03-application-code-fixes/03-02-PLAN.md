---
phase: 03-application-code-fixes
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/mega_planner/receipts/trip.ex
  - server/lib/mega_planner/receipts/stop.ex
  - server/lib/mega_planner/receipts/purchase.ex
  - server/lib/mega_planner/receipts/tax_indicator_meaning.ex
  - server/lib/mega_planner/receipts/store.ex
  - server/lib/mega_planner/receipts/driver.ex
  - server/lib/mega_planner/receipts/brand.ex
  - server/lib/mega_planner/receipts/format_correction.ex
  - server/lib/mega_planner/calendar/task_step.ex
  - server/lib/mega_planner/calendar/task.ex
  - server/lib/mega_planner/calendar/task_template.ex
  - server/lib/mega_planner/goals/habit.ex
  - server/lib/mega_planner/goals/goal_status_change.ex
  - server/lib/mega_planner/goals/goal.ex
  - server/lib/mega_planner/goals/habit_inventory.ex
  - server/lib/mega_planner/goals/goal_category.ex
autonomous: true
requirements:
  - CODE-03

must_haves:
  truths:
    - "No foreign_key_constraint calls remain in any receipts/ calendar/ goals/ schema file"
    - "Each removed FK constraint is replaced with validate_change/3 that does a Repo.get for the referenced module"
    - "Files compile without errors after changes"
  artifacts:
    - path: "server/lib/mega_planner/receipts/trip.ex"
      provides: "validate_change/3 for household_id, user_id, driver_id"
      contains: "validate_change"
    - path: "server/lib/mega_planner/calendar/task.ex"
      provides: "validate_change/3 for user_id, household_id, parent_task_id"
      contains: "validate_change"
    - path: "server/lib/mega_planner/goals/goal.ex"
      provides: "validate_change/3 for household_id, goal_category_id"
      contains: "validate_change"
  key_links:
    - from: "server/lib/mega_planner/receipts/trip.ex"
      to: "MegaPlanner.Repo.get/2"
      via: "validate_change/3 pre-insert check"
      pattern: "validate_change"
---

<objective>
Replace all `foreign_key_constraint/3` calls in receipts, calendar, and goals schema modules with `validate_change/3` pre-insert existence checks using `Repo.get/2`.

Purpose: `foreign_key_constraint/3` is silently broken on ecto_sqlite3 — SQLite returns `[foreign_key: nil]` and Ecto raises an unhandled `Ecto.ConstraintError` (a 500 error) instead of a changeset validation error. Replace with pre-insert checks that fail gracefully.

Output: 16 files updated with validate_change/3 blocks replacing foreign_key_constraint calls. 27 FK constraint calls replaced across receipts (14), calendar (6), and goals (7) domains.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-application-code-fixes/03-RESEARCH.md

<interfaces>
<!-- FK constraint replacement pattern (from ecto_sqlite3 docs and research) -->
<!-- SQLite returns [foreign_key: nil]; Ecto.Repo.Schema raises ConstraintError on nil -->
<!-- Pattern: remove foreign_key_constraint, add validate_change/3 doing Repo.get -->

<!-- BEFORE (silently broken on SQLite): -->
<!--   |> foreign_key_constraint(:household_id) -->

<!-- AFTER (works in both): -->
<!--   |> validate_change(:household_id, fn :household_id, id -> -->
<!--        if MegaPlanner.Repo.get(MegaPlanner.Households.Household, id), -->
<!--          do: [], -->
<!--          else: [household_id: "does not exist"] -->
<!--      end) -->

<!-- Module-to-schema map for this plan's files: -->
<!-- :household_id  -> MegaPlanner.Households.Household -->
<!-- :user_id       -> MegaPlanner.Accounts.User -->
<!-- :driver_id     -> MegaPlanner.Receipts.Driver -->
<!-- :trip_id       -> MegaPlanner.Receipts.Trip -->
<!-- :store_id      -> MegaPlanner.Receipts.Store (stop.ex) -->
<!-- :stop_id       -> MegaPlanner.Receipts.Stop (purchase.ex) -->
<!-- :budget_entry_id -> MegaPlanner.Budget.Entry (purchase.ex) -->
<!-- :task_id       -> MegaPlanner.Calendar.Task (task_step.ex) -->
<!-- :parent_task_id -> MegaPlanner.Calendar.Task (task.ex, self-referential) -->
<!-- :goal_id       -> MegaPlanner.Goals.Goal (goal_status_change.ex) -->
<!-- :goal_category_id -> MegaPlanner.Goals.GoalCategory (goal.ex) -->
<!-- :parent_id     -> MegaPlanner.Goals.GoalCategory (goal_category.ex, self-referential) -->

Important: validate_change/3 only fires when the field is present AND changed in the cast.
If a field is optional (nil is valid), wrap the Repo.get branch to handle nil:
  |> validate_change(:driver_id, fn :driver_id, id ->
       if MegaPlanner.Repo.get(MegaPlanner.Receipts.Driver, id),
         do: [],
         else: [driver_id: "does not exist"]
     end)
This is only called when driver_id is non-nil and present in the changeset.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace FK constraints in receipts schemas (8 files)</name>
  <files>
    server/lib/mega_planner/receipts/trip.ex,
    server/lib/mega_planner/receipts/stop.ex,
    server/lib/mega_planner/receipts/purchase.ex,
    server/lib/mega_planner/receipts/tax_indicator_meaning.ex,
    server/lib/mega_planner/receipts/store.ex,
    server/lib/mega_planner/receipts/driver.ex,
    server/lib/mega_planner/receipts/brand.ex,
    server/lib/mega_planner/receipts/format_correction.ex
  </files>
  <action>
Read each file, then rewrite it replacing every `foreign_key_constraint/3` call with the equivalent `validate_change/3` block. Use `Repo.get(Module, id)` — return `[]` if found, `[field: "does not exist"]` if nil.

**trip.ex** — replace 3 FK constraints:
- `:household_id` → `MegaPlanner.Households.Household`
- `:user_id` → `MegaPlanner.Accounts.User`
- `:driver_id` → `MegaPlanner.Receipts.Driver` (optional field — validate_change only fires when non-nil)

**stop.ex** — replace 2 FK constraints:
- `:trip_id` → `MegaPlanner.Receipts.Trip`
- `:store_id` → `MegaPlanner.Receipts.Store`

**purchase.ex** — replace 3 FK constraints:
- `:household_id` → `MegaPlanner.Households.Household`
- `:stop_id` → `MegaPlanner.Receipts.Stop`
- `:budget_entry_id` → `MegaPlanner.Budget.Entry` (optional field)

**tax_indicator_meaning.ex** — replace 1 FK constraint:
- `:household_id` → `MegaPlanner.Households.Household`

**store.ex** — replace 1 FK constraint:
- `:household_id` → `MegaPlanner.Households.Household`

**driver.ex** — replace 2 FK constraints:
- `:household_id` → `MegaPlanner.Households.Household`
- `:user_id` → `MegaPlanner.Accounts.User`

**brand.ex** — replace 1 FK constraint:
- `:household_id` → `MegaPlanner.Households.Household`

**format_correction.ex** — replace 1 FK constraint:
- `:household_id` → `MegaPlanner.Households.Household`

Total: 14 FK constraints replaced in receipts domain.

Do NOT remove `import Ecto.Changeset` — it is needed for validate_change/3.
Do NOT add a new `alias MegaPlanner.Repo` if one is already present or if the module uses the full `MegaPlanner.Repo` path (check each file first).
  </action>
  <verify>
    <automated>grep -rn "foreign_key_constraint" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/mega_planner/receipts/ | wc -l</automated>
  </verify>
  <done>Command returns 0. All 14 foreign_key_constraint calls in receipts/ replaced with validate_change/3. `cd /Users/sn0w/Documents/dev/lifeboard-mono/server && mix compile` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Replace FK constraints in calendar and goals schemas (8 files)</name>
  <files>
    server/lib/mega_planner/calendar/task_step.ex,
    server/lib/mega_planner/calendar/task.ex,
    server/lib/mega_planner/calendar/task_template.ex,
    server/lib/mega_planner/goals/habit.ex,
    server/lib/mega_planner/goals/goal_status_change.ex,
    server/lib/mega_planner/goals/goal.ex,
    server/lib/mega_planner/goals/habit_inventory.ex,
    server/lib/mega_planner/goals/goal_category.ex
  </files>
  <action>
Read each file, then rewrite it replacing every `foreign_key_constraint/3` call with the equivalent `validate_change/3` block. Use `Repo.get(Module, id)` — return `[]` if found, `[field: "does not exist"]` if nil.

**calendar/task_step.ex** — replace 1 FK constraint:
- `:task_id` → `MegaPlanner.Calendar.Task`

**calendar/task.ex** — replace 3 FK constraints:
- `:user_id` → `MegaPlanner.Accounts.User`
- `:household_id` → `MegaPlanner.Households.Household`
- `:parent_task_id` → `MegaPlanner.Calendar.Task` (self-referential, optional)

**calendar/task_template.ex** — replace 1 FK constraint:
- `:household_id` → `MegaPlanner.Households.Household`

**goals/habit.ex** — replace 1 FK constraint:
- `:household_id` → `MegaPlanner.Households.Household`

**goals/goal_status_change.ex** — replace 2 FK constraints:
- `:goal_id` → `MegaPlanner.Goals.Goal`
- `:user_id` → `MegaPlanner.Accounts.User`

**goals/goal.ex** — replace 2 FK constraints:
- `:household_id` → `MegaPlanner.Households.Household`
- `:goal_category_id` → `MegaPlanner.Goals.GoalCategory` (optional field)

**goals/habit_inventory.ex** — replace 1 FK constraint:
- `:household_id` → `MegaPlanner.Households.Household`

**goals/goal_category.ex** — replace 2 FK constraints:
- `:household_id` → `MegaPlanner.Households.Household`
- `:parent_id` → `MegaPlanner.Goals.GoalCategory` (self-referential, optional)

Total: 13 FK constraints replaced in calendar and goals domains.

Note for task.ex: the existing `unique_constraint/3` call with `:name` option should be preserved unchanged — only remove the `foreign_key_constraint` lines.
  </action>
  <verify>
    <automated>grep -rn "foreign_key_constraint" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/mega_planner/calendar/ /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/mega_planner/goals/ | wc -l</automated>
  </verify>
  <done>Command returns 0. All 13 FK constraints in calendar/ and goals/ replaced with validate_change/3. `cd /Users/sn0w/Documents/dev/lifeboard-mono/server && mix compile` succeeds.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "foreign_key_constraint" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/mega_planner/receipts/ | wc -l` returns 0
2. `grep -rn "foreign_key_constraint" /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/mega_planner/calendar/ /Users/sn0w/Documents/dev/lifeboard-mono/server/lib/mega_planner/goals/ | wc -l` returns 0
3. `cd /Users/sn0w/Documents/dev/lifeboard-mono/server && mix compile` exits 0 with no errors
</verification>

<success_criteria>
- 27 foreign_key_constraint calls removed from receipts, calendar, and goals schema files
- All replaced with validate_change/3 doing Repo.get pre-insert existence checks
- mix compile succeeds across all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/03-application-code-fixes/03-02-SUMMARY.md`
</output>
